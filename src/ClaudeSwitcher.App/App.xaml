<Application x:Class="ClaudeSwitcher.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:ClaudeSwitcher.App"
             ShutdownMode="OnExplicitShutdown">
    <Application.Resources>
        <SolidColorBrush x:Key="BgDark"   Color="#1E1E2E"/>
        <SolidColorBrush x:Key="BgMid"    Color="#2A2A3E"/>
        <SolidColorBrush x:Key="BgCard"   Color="#313149"/>
        <SolidColorBrush x:Key="FgMain"   Color="#E0E0F0"/>
        <SolidColorBrush x:Key="FgDim"    Color="#888899"/>
        <SolidColorBrush x:Key="Accent"   Color="#D97706"/>
        <SolidColorBrush x:Key="Green"    Color="#22C55E"/>
        <SolidColorBrush x:Key="Red"      Color="#EF4444"/>

        <Style TargetType="Button" x:Key="AccentButton">
            <Setter Property="Background" Value="{StaticResource Accent}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="14,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="GhostButton" BasedOn="{StaticResource AccentButton}">
            <Setter Property="Background" Value="{StaticResource BgMid}"/>
            <Setter Property="Foreground" Value="{StaticResource FgMain}"/>
        </Style>
    </Application.Resources>
</Application>
